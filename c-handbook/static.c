#include <stdio.h>
/* In this program 1 is always returne */
int main(void) {
    int incrementAge() {
   /* int age  = 0;
    age++;
    return age;
    }
}

/* In the program below the value increase by 1 */
    static int age;
    age++;
    return age;
    }
    printf("%d\n", incrementAge());
    printf("%d\n", incrementAge());
    printf("%d\n", incrementAge());
}

/*static arrays*/
    static int ages[3];
    ages[0]++;
    return ages[0];
}
